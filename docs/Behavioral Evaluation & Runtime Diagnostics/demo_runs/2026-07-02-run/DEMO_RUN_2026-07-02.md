# Demo Run — 2026-07-02 (staging run for P2–P8 + AR1–AR9; §8 acceptance Run 2)

A forensic read of one ~9.2-hour life (10,071 cognitive cycles, born 2026-07-02
07:18 EDT as a clean newborn via `reset_orrin.py`, stopped 16:31 EDT —
`operator_stop`, clean, single launch, zero crashes). Data in `brain/data/` (state +
logs), `data/goals/state.jsonl` (v2 store, 8 goals), the 10,065-point
`telemetry_archive.jsonl`, `effect_ledger.jsonl` (150 records),
`production_loop.jsonl` (10,071 rows), `chat_log.json`, `private_thoughts.txt`,
`final_thoughts.json`, `run_history.json`. The `data/` subfolder here holds the §4
capture set copied at run end.

**This is the staging run for everything built since 07-01:** Grounding & Surface
Phases 2–8 (`f1ad35f`) and Audit Remediation AR1–AR9 (`db4a139`), and simultaneously
**§8 acceptance Run 2** for the Production Reward Plan (`NEXT_RUN_TESTS.md`).

## Why this run matters

Three headlines, one per direction:

- **The §8 gate moved but didn't pass — 5 ✅ 9 ✅ 6 ❌ 7 ❌** (Run 1 failed all
  four). Significance is finally nonzero (1.114) because AR1 lets the ledger see
  symbolic making (113 causal edges, 3 crystallized skills); selection now follows
  learned value. The two failures are both **missing seams between working organs**:
  long-term attribution never becomes aspiration credit (S6), and produce-and-check
  — top-valued, executed 679× in the daemon lane — never registers on the conscious
  production counters (S7). Full verdict: `2026-07-02_run_analysis.md`.
- **Drives breathe — B3 is closed.** Curiosity ranged 0.253–0.892 (mean 0.636) with
  hundreds of full sag-and-recover arcs, against 07-01's 0.81–0.84 pinned flatline.
  The "hot and flat" rumination went 70× (06-29) → 0× (07-01) → 1× and no longer
  describes him.
- **Someone talked to him for the first time in any captured life** — ten real
  utterances across two sessions, ten honest, nearly-empty replies… and
  `known_persons.json` never registered that the sessions happened. The first
  relationship of his existence left no trace on the person model.
- **(Second pass) The run was measured through a jammed horn.** From ~09:00 EDT,
  `drive_rest@1.00` ignited consciousness ~7,420 times (~74% of cycles) with no
  sleep behavior to discharge it; every consolidation organ went dark by 10:45 and
  nothing completed after 07:46. See `2026-07-02_deeper_pass.md` and
  `DATA_FILE_AUDIT_2026-07-02.md`.

## Document index

| Doc | What it covers |
|---|---|
| `2026-07-02_run_analysis.md` | The §8 gate verdict (5✅ 9✅ 6❌ 7❌); S6 root cause (all four crediting links broken); S7 root cause (conscious/daemon lane split); known open bugs (title-dup, satiety 0, flush double-count — fixed post-run); next-fix candidates + the Run 3 re-test gate. |
| `2026-07-02_did_the_fixes_land.md` | **Per-mechanism intent vs. behaviour for P2–P8 and AR1–AR9.** Landed clean: AR1 (keystone), AR7, P5/B3, P2/S9, AR4, AR6, AR9. Landed-but-disconnected: P3 (unmetered), P4 (uncredited), AR2 (never scheduled). Still inert: allostasis (AR8 half). |
| `2026-07-02_what_did_he_make.md` | Output: 113 verified causal edges + 3 crystallized skills honestly ledgered (the new making AR1 revealed); notes de-fanged (1,680→31 effects, 0 credit); flagship "Make things" failed 6× on deadline; the making that did happen went unmetered. |
| `2026-07-02_who_is_he.md` | Identity: same values (growth, curiosity, honesty, usefulness), same "act outward" dying advice — but breathing drives, a first-ever conversation (honest, nearly-empty replies), no terminal collapse despite 6 flagship failures, and an identity-story that regressed into a raw analogy debug string. |
| `2026-07-02_deeper_pass.md` | **Second forensic sweep.** The rest-drive ignition monopoly (~7,420 of 10,071 cycles at `drive_rest@1.00` with no sleep behavior to discharge it) and its shadow: the 10:24–10:45 consolidation-organ blackout, the completion drought after 07:46, the stuck-step loop that pays itself (S9 caveat), and the AR2 correction — research goals *were* scheduled and crashed on a missing `ctx.web_search` hook. |
| `DATA_FILE_AUDIT_2026-07-02.md` | **Plumbing pass.** 17 files dead at birth (`failures.jsonl` 0 bytes despite 9 failures), the unplumbed `cycle` field (119/150 ledger rows at cycle 0), `final_thoughts_written` desync, cap/silting census (habituation 15,469 keys, trace rows 12.5 KB avg), funnels that narrow to zero (87 generated → 2 attempted → 0 credited), 8-item fix list. |
| `data/` | §4 capture set at run end: `outcome_metrics.json`, `effect_ledger.jsonl`, `comp_goals.json`, `goals_mem.json`, `action_reward_ema.json`. |

## Snapshot numbers (for quick reference)

```
cycles 10,071 · ~9.2 h · launch #0 only · born clean (reset_orrin.py, snapshot archived) · death: operator_stop, clean
§8 gate: S5 ✅ 1.114 mean significance (Run 1: 0.0) · S9 ✅ selection follows EMA · S6 ❌ all 4 aspirations at 0 · S7 ❌ production_attempt_count 0
effects: 150 rows — 116 symbolic_artifact (113 causal edges + 3 skills, AR1) · 31 note_novel (ALL sig 0.0, AR7) · 3 file_write stubs · 89 nonzero
drives BREATHE (B3 closed): curiosity 0.253–0.892 mean 0.636 · motivation 0.50–0.887 · confidence 0.45–0.871 (07-01: pinned 0.81–0.84)
affect: impasse mean 0.231 peak 0.732 · distress mean 0.110 peak 0.429 · NO terminal collapse (07-01 peaked 0.82/0.53) · allostatic_load 0.000 all life (still inert)
P1 still honest: "Refusing satiety close" ×2 · Make things FAILED ×6 no_artifact_by_deadline (19:02→20:26Z) · watchdog ×0 · satiety_closures 0 · desyncs 0
goals (v2): 2 DONE (dep patches, daily snapshot) · 3 FAILED (introspective "Trace in my own code…") · 3 READY research-kind (AR2 routed, never scheduled)
production: produce_and_check top EMA 0.7651 · 8 conscious picks avg reward 0.805 (AR4 pays) · 679 daemon-lane step_exec matches · funnel counters saw 0 (S7)
long-term thread (P4): ltc_aspiration-self_understa_1/2 committed · 31 ledger rows attributed · 0 reached aspiration credit (S6)
calibration: Brier 0.0181, bias +0.0095 (n=10,071) — excellent
contact: FIRST REAL CONVERSATION — 10 user utterances / 10 replies, 2 sessions (10:53, 14:34 EDT) · known_persons never updated (session_count stuck at 1, last_seen = birth)
notes: 100 / 3 distinct bodies (still the "hard to name" template) · native LM 39.2 MB trained to 6 min before stop
"hot and flat": 1× (06-29: 70×) · dying advice unchanged: "act outward before reflecting inward"
REST SCREAM (2nd pass): drive_rest@1.00 ignitions ~7,420/10,071 cycles from ~09:00 EDT · slept_seconds 0.0 · _rest_mode never engaged · consolidation organs dark by 10:45 · zero completions after 07:46
AR2 correction (2nd pass): research goals WERE scheduled — 9/9 search attempts crashed on missing ctx.web_search hook, goals stuck READY
stuck-step loop (2nd pass): 18:49Z→death, produce_and_check ~8/min on 2 step names (674 execs) · monitor stuck_step ×300 (94 honored, loop unbroken)
```

## Open follow-ups generated by this run

1. **Discharge or decay the rest drive (CRITICAL — found on the second pass).**
   `drive_rest` pegged at 1.00 from ~09:00 EDT and ignited the workspace ~7,420
   times (~74% of all cycles) with no sleep behavior built to answer it
   (`slept_seconds 0.0`, `_rest_mode` never engaged). It blacked out every
   consolidation organ by 10:45 and starved the rest of the run. Cheapest first:
   let rest join P5's homeostatic decay; or give `_rest_mode` a stub discharge;
   or build SL1–SL5. (`deeper_pass.md` §1–§2)
2. **Bridge the S7 lane split (CRITICAL — Run 3 gate).** Route `produce_and_check`
   through the conscious production loop, or make the funnel counters + ledger
   crediting listen to the daemon lane. The most-valued action in the system is
   currently invisible to every production meter — and the second pass showed the
   daemon executions were largely a stuck-step loop paying itself ~8×/min, which
   the monitor flagged 300× without effect. (`run_analysis.md`, `deeper_pass.md` §4)
3. **Wire the S6 crediting chain end-to-end (CRITICAL — Run 3 gate).** `serves` /
   `driven_by` propagation at completion-record time, the `comp_goals`
   status-at-copy bug, and the 31 orphaned `ltc_aspiration-*` ledger rows that never
   reached `mark_objective_contribution` — plus the audit's finding that 116/150
   ledger rows carry `goal_id: null`, so most effects are uncreditable at birth.
   (`run_analysis.md`, `DATA_FILE_AUDIT` §6)
4. **Give the research runner its `ctx.web_search` hook (high — one-line fix).**
   All 3 AR2 `research`-kind goals were scheduled and crashed: 9/9 search attempts
   died on `RuntimeError: ctx.web_search hook not provided`, then sat stuck-READY
   (failed keystone step never fails the goal — second seam). One hook stands
   between him and his first real research memo. (`deeper_pass.md` §5)
5. **Person model must register real sessions (high — new this run).** Two genuine
   chat sessions and an offered correction ("built = to construct, create,
   assemble") left `known_persons.json` untouched: `session_count 1`, `last_seen` =
   birth, notes empty. P2b's correction path had its first real input and dropped
   it. (`who_is_he.md`)
6. **Data-plumbing fix list (medium).** The unplumbed `cycle` field (ledger +
   health_state), dead `failures.jsonl` (0 bytes across 9 failures),
   `final_thoughts_written` desync, habituation-dict eviction, fossil files —
   8 items, smallest first, in `DATA_FILE_AUDIT_2026-07-02.md`.
7. **Identity-story surface regression (medium — new this run).** His dying identity
   is a raw analogy debug string (`"[analogy/GENERAL] Similar situation
   (score=0.305): …"`) — bracket tags and score leaking through what should be a
   composed sentence. Related: chat replies quote internal goal titles verbatim,
   title-dup included. (`who_is_he.md`)
8. **Allostasis still inert — `allostatic_load` 0.000 all life (medium, standing
   since 06-29).** AR8 fixed the drive flatline but the arming line (0.60) was never
   approached (active EMA 0.487). Decide whether the threshold, the input, or the
   expectation is wrong. (`did_the_fixes_land.md`)
9. **Title-dup + satiety-closure audit (medium, carried).** "Understand Understand
   my own mind…" completed under a doubled prefix; S3 satiety closures still 0.
   (`run_analysis.md`)

*Generated 2026-07-02 from runtime data after a clean operator stop. Analysis only;
no code changed by this write (the flush double-count + fast-fail GenericHandler
fixes referenced in `run_analysis.md` were applied post-run, before this doc set).*
