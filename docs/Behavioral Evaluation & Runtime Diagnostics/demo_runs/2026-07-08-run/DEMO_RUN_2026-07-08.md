# Run 5 — 2026-07-08 life — §8 verdict

**Life:** born 2026-07-08 08:42:58 local, operator SIGTERM at 20:22 (2026-07-09T00:22:30Z),
**12,330 cycles in ~11.7 h, single clean segment, zero crashes, zero daemon desyncs.**
Code = `f0c4698` (F1–F22). Clean `reset_orrin` before launch. Evidence: this folder;
manifest `RUN_CAPTURE_2026-07-08.md`.

## Verdict: gate NOT passed — but the Run 5 build gate (F1/F2/F3) largely landed

This is the cleanest skeleton yet and the two hardest 07-05 blockers are fixed: the
197 KB template stamper is gone and aspirations survived the whole life. But three of
the nine acceptance signals still fail (**S6, S9, and — newly visible — S3's meter**),
and the run exposes a **new dominant pathology the earlier "jammed horn" fixes did not
reach: a committed-goal monopoly.** 99.9 % of the last 3,000 decisions were committed to
one aspiration (`self_understanding`), which collected 131 of 152 credited effect rows.
The horns that jammed on *ignition* in Runs 2–4 now jam one layer up, on *commitment*.

## Run 5 build gate (the F1/F2/F3 the fix doc promised)

| Fix | Promise | Result |
|---|---|---|
| **F1** compose_section grounded-or-failed, no template stamp | no 197 KB stamp; EMA moves; no step retried >5× w/o status change | ✅ **mostly** — stamper gone; `compose_section` ran 166× and appears in the EMA; **1** `steps_unreachable: 3 steps abandoned at the 3-attempt cap` proves F1b's durable counter fires. ⚠️ but the **offline synthesis fallback recursively nests prior memos verbatim** (a memo quotes a memo quoting a memo) — grounded, but degenerating. |
| **F2** aspirations editable, never failed | 0 failure rows with `aspiration-` goal_id; all four present at death | ✅ **PASS** — `failures.jsonl` has **0** aspiration rows; all four aspirations present in `goals_mem.json` at death. The 07-05 killer (output_producing failed 104 s before death) did not recur. |
| **F3** note bodies are artifacts, not memories | credited bodies resolvable in sidecar | ✅ **PASS** — 106 `effect_artifacts/` sidecars; 5 `note_novel` rows carry real 1,166–1,659-char bodies; `"Wrote a 'what I learned' note … satiety close can now complete legitimately"` logged 5×. |

## The nine §8 signals

| # | Signal | Run 5 | vs Run 3 | Result |
|---|---|---|---|---|
| 1 | Fewer repeated understanding goals | 13 completed / 11 distinct titles; max repeat **2×**; 0.16 /1k cycles | better (was 0.44/1k) | 🟢 **PASS** |
| 2 | Nonzero goal duration | median **42.2 s** | **collapsed** from 3,722 s | 🟡 **meter passes, meaning fails** — goals close seconds after one research step; the number is >0 but it means *shallower*, not *sustained*. |
| 3 | Nonzero satiety closures | metric **0**, but **7** `closed (satiety:…)` in the logs | mechanism now fires (F3) | 🔴 **METER BROKEN** — `satiety_closures` counter is not wired to the close path; the closures are real. |
| 4 | Some legitimate failures | 32 rows; **20 = `ValueError: no URLs to fetch`**, 1 = `steps_unreachable`, rest step/objective | more, but same defect | 🟡 — failures are real but 63 % are one broken capability (search returns 0 URLs), not healthy deadline pressure. |
| 5 | Higher mean significance | **1.176** tier-weighted / **0.385** raw credited | held (was 1.197) | 🟡 **HELD but inflated** — 116/152 credited rows are `symbolic_artifact` = *"causal edge established"*, which §6.4 says is **not** production material. |
| 6 | Aspiration diversity | contributions **1 / 2 / 0 / 6**; `genuine_contact` **0**; top share **67 %** | regressed (Run 3 passed) | 🔴 **FAIL** — one aspiration at 0 %, top > 60 %; both targets missed. |
| 7 | Artifacts useful / reused | **8 `reuse` rows** (Run 3: 0); real memo-builds-on-memo arc (biology memo cites history memo) | **first-ever reuse** | 🟢 **MOVED — biggest gain** — `mark_reused` now has call sites; caveat: reuse rows are char_len-0 tier-3 markers, not re-read bodies. |
| 8 | No resurrection / orphan-RUNNING | `store_desyncs_repaired` **0** | clean (Run 3: 12) | 🟡 **PASS w/ asterisk** — daemon reports 0, but 2 research goals go **FAILED→DONE** in the WAL (a fetch-failed goal still "completes" via its synthesis child). Not a v1/v2 desync, but a status the failure path shouldn't be able to override. |
| 9 | Selection follows learned value | **corr(EMA, share) = −0.17**; `look_outward` has the **worst** reward (0.228) yet is picked #6 (7.7 %) | still negative | 🔴 **FAIL** — the A4 multiplicative EMA authority did not flip the sign; multi-factor weights (`emo 0.312 > goal 0.297`) still outvote learned value. |

**Decision rule (from `NEXT_RUN_TESTS.md`):** S8 or S9 failing makes the fix cosmetic
regardless of 1–7. **S9 fails.** Gate not passed.

## What actually changed (the honest gain)

Runs 1–4 could not *make* anything real, or made a fake 197 KB manuscript. Run 5 makes
**real, grounded, reused artifacts** — four research memos and three syntheses, each
citing Wikipedia sources and prior memos by path, with resolvable bodies. That is the
first time the production organ produced content a human could read and trace. The
skeleton (no desyncs, aspirations survive, bodies persist) is sound.

## The four blockers for Run 6

1. **Committed-goal monopoly** — 99.9 % of decisions committed to one aspiration; credit,
   attention, and production all collapse onto `self_understanding`. This is the Run-6
   headline. (Detail: `2026-07-08_goals_system_audit.md`.)
2. **Production credit is 76 % causal-graph edges** — `symbolic_artifact` "causal edge
   established" rows are counted as made things; they are self-model bookkeeping. Split
   them out of the production/significance denominator.
3. **S9 EMA still has no authority** — the multiplicative scaler is outvoted by affect;
   `look_outward` keeps high share at the lowest reward.
4. **Memory is 91 % working-memory-summary spam** (1,825 / 2,001 long-memory entries) —
   the making organ works but the memory estate it draws on is drowning in instrumentation.

See `2026-07-08_run_analysis.md` (forensics + all eleven §6 checks) and
`2026-07-08_goals_system_audit.md` (the goals-system deep dive).
