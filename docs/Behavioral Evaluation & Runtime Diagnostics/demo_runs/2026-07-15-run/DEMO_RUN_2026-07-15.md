# Run 8 Verdict — 2026-07-15 life

*Eighth acceptance life. Analysis + gate verdict for the capture in
`RUN_CAPTURE_2026-07-15.md`. Build: `RUN8_FIX_PLAN_2026-07-14.md` F1+F2
(`fc2b635`), plus an uncommitted mid-life `invoke.py` crash fix.*

## Verdict: the monopoly finally broke — **but by F2, not F1, and the gate as written does not return PASS**

Six straight runs could not move the committed-goal monopoly off ~90 %. **Run 8
moved it to 42.6 %.** All four aspirations drove the slot; `genuine_contact` held
it for the first time ever. That is the headline the gate was built around, and
it is real in **both** runtime segments (54 % pre-crash, 44 % post-fix — both
under the 60 % line).

But the cause is not the lever the gate scores. **F1 (the absolute staleness
refractory) never fired** — no goal ever went stale long enough to trip it,
because **F2 (admitting all four aspirations to the rotation pool) kept the slot
turning over so staleness never accumulated.** Prevention beat cure. The gate's
`G2` demands the release *actually fire* (`refractory_events` non-empty); it is
empty. So:

- **Objective: MET.** Monopoly broken, no thrash, no idleness, production up.
- **Formal gate (`Pass = G1 ∧ G2 ∧ G4`): NOT passed** — `G2` unmet (F1 never
  triggered), and `G5` regressed on reuse (2 < Run 7's 4).
- **F1 remains unproven in life.** The `ORRIN_STALE_REFRACTORY=0` ablation arm is
  now the priority, to confirm F2 is the operative lever (and whether F1 is a
  still-needed backstop or dead weight).

This life also **exposed and fixed a real crash** (`make_candidate` keyword-only
dispatch, §0) — the single most consequential engineering event of the run.

---

## 0. The crash (mid-life) — what the user flagged

The `silent_death` in `lifecycle_events.jsonl` was **not** a natural death: it
was an uncaught `TypeError` that killed the brain thread at cycle ~4253, after
which the process pulsed as a zombie for 6.5 h until the watchdog noticed.

```
TypeError: make_candidate() missing 2 required keyword-only arguments:
           'kind' and 'direction'      (brain/loop/invoke.py:108, fn(**built))
```

`_invoke_cognition`'s dispatchability guard checked `POSITIONAL_ONLY` and
`POSITIONAL_OR_KEYWORD` params but not required `KEYWORD_ONLY` params, so a
function with `(*, kind, direction)` looked callable and blew up on call.
The **uncommitted fix** adds `KEYWORD_ONLY` to the guard; segment 2 then logged
*"make_candidate … not directly dispatchable; skipping"* and ran crash-free.

**Follow-ups:** (a) commit the `invoke.py` fix with a regression test that a
required-keyword-only cognitive function is skipped, not dispatched; (b) the
watchdog took 6.5 h to notice a dead cognitive loop while pulses continued — the
liveness check keys on process pulse, not cycle advance. Worth a shorter
cycle-stall tripwire. Full trace: `logs/CRASH_TRACEBACK.txt`.

---

## 1. Acceptance signals (the nine + S9/S10)

Read from `outcome_metrics.json`, `comp_goals.json`, `aspiration_scoreboard.json`,
`effect_ledger.jsonl`, `commitment_signals.json`, `decision_stats.json`,
`production_loop.jsonl`.

| # | Signal | Run 8 | vs target | Result |
|---|---|---|---|---|
| 1 | Fewer repeated understanding goals | **16 completed, 13 distinct titles**, max 2× | repeat rate ↓, none > 2×/1k | ✅ **PASS** |
| 2 | Nonzero goal duration | `median_seconds_to_complete` **112.7 s** (Run 7: 49.5) | > 0 | ✅ (up) |
| 3 | Nonzero satiety closures | `satiety_closures` **16** (Run 7: 12) | > 0 | ✅ |
| 4 | Legitimate failures | `goals_failed` **7** (traceable — e.g. "emotional response triggered") | > 0, traceable | ✅ |
| 5 | Higher mean significance | `mean_significance` **1.242** (Run 7: 1.205); 23 nonzero ledger rows, max 1.0 | clearly > 0 | ✅ |
| 6 | Aspiration **contribution** diversity | completions producing **11** / self 2 / world 1 / **contact 0**; top **78.6 %** | none at 0 %, top < 60 % | 🔴 **FAIL** — contact still 0 (4th run); concentration merely *moved* from self→producing |
| 7 | Artifacts reused | **2** reuse rows, both `path:null ref:null` (Run 7: 4) | ≥ 8, concrete | 🔴 **REGRESSED** (fewer, still no referent) |
| 8 | No resurrection / desync | `store_desyncs_repaired` **0** | ~0 and stays | ✅ **4th clean run** |
| 9 | Selection follows value | `look_outward` count 755, avg reward **0.287** (low, demoted); `research_topic` #1 at avg **0.651** | low-value action demoted | ✅ / 🟡 |
| 10 | **Anti-monopoly (commitment layer)** | driver top **42.58 %** (seg1 54.2 % / seg2 43.8 %); **all 4 aspirations drove**; `genuine_contact` 1.35 % (Run 7: 0) | no channel > ~60 % | ✅ **PASS — the headline finally moved** |

**Note on S6 vs S10.** These measure different layers. S10 (which *commits* the
driver slot) was the disease and is now cured. S6 (which *contributes* completed
work) is still lopsided — but it flipped from self_understanding to
**output_producing**, i.e. the "make things" aspiration is now the one actually
making things (11 completions). The persistent hole is `genuine_contact`: 0
contributions for the fourth straight run, though it at least *drove* the slot
this life (132 cycles, all pre-crash).

---

## 2. The Run 8 gate — G1–G5

Gate text: `NEXT_RUN_TESTS.md` "Run 8 re-test gate". **Pass = G1 ∧ G2 ∧ G4, with
G5 no regression.**

| Gate | Requirement | Run 8 | Result |
|---|---|---|---|
| **G1** | No committed goal > ~60 % of life cycles | **42.58 %** whole-life; 54.2 % / 43.8 % per segment — all < 60 % | ✅ **PASS** (the number six runs never moved: 90.9 → 42.6) |
| **G2** | `refractory_events` non-empty (release fired) **and** max `stale_cycles` < ~550 | `refractory_events` **absent**; all `recommit_block_pulls` = 0; **F1 never tripped**. Second clause ✅ (max stale 8.8). | ❌ **NOT MET as written** — but because staleness never *accumulated* (F2 pre-empted it), not because F1 is broken. F1 is **unproven**, not failed. |
| **G3** | Release latency ≤ ~300; no `avoid_streak` in thousands | longest hold **75** cycles; max `avoid_streak` **5.6**; 2,094 transitions | ✅ (occupancy fully fluid) |
| **G4** | No refractory event on a credit-earning goal (anti-thrash) | none fired → F1 never punished productive work | ✅ (vacuously) |
| **G5** | HOLDS: no value re-pump; desync 0; 4 aspirations survive; reuse ≥ Run 7; production not collapsed | value_ema ≤ 0.625 (no pump) ✅; desync 0 ✅; all 4 survive ✅; **reuse 2 < 4** ❌; production up (11 completions, 21 artifacts, 15 novel notes) ✅ | 🟡 **holds except reuse regressed** |

**Decision-rule result:** `G1 ✅ ∧ G2 ❌ ∧ G4 ✅` → **gate NOT passed as written.**
The one thing the gate insisted on proving — that the F1 release fires — this run
could not prove, because the monopoly it was built to break never re-formed.

---

## 3. Why the monopoly broke — F2 did it, F1 stood down

`RUN8_FIX_PLAN` diagnosed two reinforcing causes of the Run-7 monopoly:
(1) all commitment authority is *relative* and saturates with no rival → **F1**
(absolute release); (2) the directional rotation pool had exactly one member →
**F2** (admit all four aspirations).

The capture shows **cause (2) was the load-bearing one.** With four roughly
equal-value directions (`value_ema` 0.50–0.625) admitted to the pool, the driver
slot rotates every few cycles among them — so no single goal ever sits idle long
enough to accrue the `stale_cycles` (250) that would arm F1. F1's trip condition
was never reached; its machinery (`commitment_value.py:162-169`) is present and
correct but dormant all life.

This is a **better** outcome than the gate anticipated (prevention over forced
release — no clock-driven churn, `G4` never at risk), but it means F1 is
**untested in a live loop**. If a future run ever produces a genuine
single-occupant slot (e.g. three aspirations retired), we still do not know F1
fires. **The ablation arm is now mandatory, not optional.**

---

## 4. What genuinely improved (do not lose these)

- **S10 monopoly 90.9 % → 42.6 %** — the headline that six runs could not move.
- **All four aspirations drove the slot**; `genuine_contact` committed for the
  first time in the series (132 cycles).
- **Production is diversifying and real:** output_producing completed 11 goals
  and led segment 2's driver share (43.8 %); 21 concrete artifacts, 15 genuinely
  novel notes (anti-pump credit from Run 7 holding — value never re-pumped).
- **4th consecutive desync-free life** (S8).
- **A real dispatcher crash was found and fixed** (§0) — latent since
  keyword-only cognitive functions existed.

## 5. Still open → Run 9

1. **F1 unproven (G2).** Run the `ORRIN_STALE_REFRACTORY=0` ablation. If
   occupancy stays < 60 % with F1 off, F2 is confirmed as the lever; decide
   whether to keep F1 as a backstop or retire it.
2. **Commit the `invoke.py` crash fix + regression test** (§0). Currently
   uncommitted; the run only survived because it was hand-applied at the seam.
3. **Watchdog stall latency** — 6.5 h to notice a dead cognitive loop. Add a
   cycle-advance tripwire distinct from process-pulse liveness.
4. **`write_exemplar` EACCES, run 3.** Not a code bug: the exemplars dir (and in
   fact the whole `docs/` subtree) is mode `0o500` — the write bit is stripped.
   `chmod u+w` the exemplars dir to actually clear this, then confirm exemplar
   promotion fires. (This same read-only-tree condition blocked *writing this
   capture* until the dirs were made writable.)
5. **Reuse regressed (2 < 4) and still referent-less** (S7); `genuine_contact`
   still contributes 0 (S6). Both are contribution-layer problems the
   commitment-layer fix did not touch — the next target once F1/F2 is settled.

## 6. Housekeeping observed in `brain/data`

- Uncommitted: `brain/loop/invoke.py` (the fix — commit it),
  `brain/data/cognitive_functions.json` + `meta_rules.json` (runtime learning
  writes into the committed seed — decide keep-vs-restore before the next reset;
  see the reset-empties-seed gotcha), and a stray `brain/data/tmpckzecsmq`
  (interrupted atomic-write temp — safe to delete).

*Written 2026-07-15 from the 2026-07-15 capture. Companions:
`RUN_CAPTURE_2026-07-15.md`, `RUN8_FIX_PLAN_2026-07-14.md`, `NEXT_RUN_TESTS.md`.*
