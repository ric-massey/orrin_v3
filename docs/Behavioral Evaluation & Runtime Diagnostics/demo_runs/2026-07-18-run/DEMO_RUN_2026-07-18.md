# Run 10 verdict — 2026-07-18/19 life

**mode: symbolic-only** (no provider key; memos are offline scrape-stitches, one
artifact self-labels "Offline synthesis fallback").
**build: `4d69ce5`** (committed, clean tree — first fully reproducible life; only
data-seed files drifted).
**shape:** single segment, **11,565 cycles**, boot stamp `run_2026_07_19_all_on`
03:12:27Z → operator SIGTERM ~17:57Z (14.6 h). Graceful pipe-safe shutdown,
run-lock released, **zero crashes, zero contiguity gaps** — the cleanest capture
of any run. `final_thoughts_written: False` (operator-stop path skips them —
check intent).

**Gate: NOT PASSED as written — S7 reuse 0 < 8 (regressed from Run 9's 1).**
But 9 of 12 gate items land green, both remaining monopoly layers stay broken,
and the binding constraint has *moved*: from honesty (Runs 5–9) to **daemon-lane
starvation** — the reuse path was never exercised because research goals barely
reached the daemon at all.

---

## Per-item scoring (Run 10 gate (a)–(l) + holds)

| Item | Score | Evidence |
|---|---|---|
| (a) RSS series + attribution | 🟢 | `resource_history.jsonl` per-cycle rss+fn all life. Sawtooth on a self-reclaiming floor: peaks 1.4→2.8 GB, floor 609→1179 MB then reclaimed to ~950. Guard never fired (robustness gate correct). Run 9's blind spot closed. |
| (b) one question = one goal id | 🟢 | 0 twin-id titles across brain tree + daemon store; 0 desyncs; 0 DONE→FAILED flaps. The Run 9 seam is dead. |
| (c) intake filed by provenance | 🟢 | `data/goals/artifacts/intake/` in use (drive-by memos filed there, not into goal dirs). |
| (d) originality veto | 🟢 | Fired twice on candidate `0799d0d3` (`verbatim_from_sources_87%`) → held for human review, not auto-canonised. **5 exemplars promoted** clean (Run 9: 3, one a pasted abstract). The bar now tells scrape from synthesis. |
| (e) run-lock at launch | 🟢 | 0 EACCES anywhere; promotions + veto prove the dir writable all life. |
| (f) daemon silence | 🔴 | **125 WAL events in 14.6 h** (72 goal_upsert + 53 step_upsert), one **8-hour** silent gap (04–12 h), 5 research-kind records, 38 state records (34 DONE). The daemon lane is starved — root cause undiagnosed (R9-F1 over-suppression, committable gate, or quotas). |
| (g) reuse ≥ 8 | 🔴 | **0 reuse rows** (Run 9: 1). Zero "reus" lines in activity logs. Downstream casualty of (f): ~5 research goals daemon-side ⇒ ~no `_find_prior_memo` calls. 25 memos exist and went uncited. **Failed fix, not interpretation** — but the failure site is upstream of the reuse machinery, which Run 9 proved works. |
| (h) reward sees impossibility | 🟡 | `decide_to_write_code` EMA **0.566, outside top-6** (Run 9: #2 at 0.618); top EMA is honest work (`read_text` 0.652). Zero-with-prejudice writes not directly verified — harness check owed. |
| (i) saturation tripwire | 🟡 | Wired (`update_signal_state.py:585`, incl. `drive_mastery` explicitly), ran every cycle, harness-tested — **0 life fires**. Cause: drive_mastery sits at 1.00 in 86 % of its ignitions but wobbles (0.84–0.96 dips), never 500 *consecutive* at the bound. Letter-correct, spirit-dodged: consecutive-streak design can't see chronic-high-with-dips. Ignition still fires 11,430/11,565 cycles (98.8 %). |
| (j) knowledge refractory | 🟢 | Max causal-edge `evidence_count` **374** (Run 9: 66,087 — 176× down); only 2 edges >100. |
| (k) first outward causal edge | 🟢 | **10 `domain: world` edges** (Run 9: 0/241): `wikipedia_search`/`fetch_and_read` → signal effects. First non-interoceptive knowledge ever. 490/500 still self — a crack, not a window. |
| (l) epistemic close-out | 🟡 | **Fired: 10 `[epistemic]` events**, every one "closed but its question was NOT answered." 17 understanding goals completed; ≥10 knowingly unanswered; stamps persist **nowhere** (LN-4 ordering + completed-goal pruning eat them — `comp_goals.json` 0 stamps, live tree 0). As built it is an annotation, not a gate: satiety still closes unanswered goals. Rung 0 exists; rung 1 (answered gates the close) is the Run 11 keystone. Question quality: all template "What is not obvious about X?" |

**Holds:** G1/S10 occupancy 🟢 **40.4 %** top (`output_producing`; ties Run 9's
best; all four aspirations committed — `genuine_contact` **84 cycles, first
nonzero commitment ever**, still funnel-invisible and out of scope per Finding 2)
· ignition-source monopoly 🟢 broken (drive_mastery 42.3 %, emotion 38.5 %,
prediction_check 10.0 %) · S4 🟡 (WAL: 0 attempts>max, 0 flaps, 0 daemon FAILED;
but LN-2 double-failures are a letter violation — see below) · S5 🟢 mean
significance 1.19/1.23 > 0 · production 🟢 **101 attempts / 50 successes / 52
artifacts** (Run 9: 37/17) · satiety meter 🟢 wired (17 closures recorded) ·
store desyncs repaired 0 🟢 · **funnel 🔴 candidate-only (149 events, zero
stages beyond)** — dead instrument, third run running.

---

## Findings, classified (broken pipe / unopposed force / misaimed force)

1. **LN-2 — `mark_goal_failed` doesn't persist** → **broken pipe.** All 4 failed
   miner goals failed exactly twice (steps_unreachable → re-adopt → 2nd site);
   9 failures.jsonl rows = 4 doubles + 1 honest single. Fix at the
   `steps_unreachable` site (same arbiter merge the retry path uses).
2. **LN-1/LN-3 — question miner harvests his own speech** → **unopposed force.**
   4 junk "Open question:" goals (sign-off echo + introspective narration) =
   every failure except one. Build the partner (provenance sense +
   researchability routing), don't delete the miner. Stream self-limited after
   ~2 h via cooldown ledger.
3. **LN-4 — close-out stamps archived before stamping** → **broken pipe.**
   `goal_closure.py`: `mark_goal_completed` (archive append) runs before
   `stamp_closeout`. Swap, or stamp inside the completion chokepoint.
4. **Daemon-lane starvation** → **cause unknown — diagnose before prescribing**
   (could be a clamp over-suppressing (R9-F1), a misaimed committable gate, or
   correct-but-unopposed brain-lane preference). This is Run 11's primary
   diagnostic target; reuse is unreachable until it moves.
5. **Close-out is an annotation, not a gate** → **misaimed force** (by design,
   rung 0). 10 goals closed on satiety with known-unanswered questions — the
   exact "closes on satiety not answered questions" pathology the
   quality-grounding design names. Promote to a gate (rung 1).
6. **Templated questions** ("What is not obvious about X?") → **misaimed
   force.** The question generator aims at format, not curiosity; the answered
   scorer said no 10/10 times it spoke.
7. **Saturation tripwire wobble-blindness** → clamp design gap: consecutive
   streak is dodged by micro-dips. Time-at-bound fraction (e.g. ≥95 % of
   trailing 500) sees what the streak can't.
8. **Funnel candidate-only** → dead instrument (3rd run) — wire the stages or
   delete them; fake coverage is worse than none.
9. **First honest real-work failure**: `Understand The Joy of Why, Kipp more
   deeply` — steps_unreachable on real reading, single failure, real reason.
   The machinery working as intended on genuine work.

## Second-pass corrections (Run 8 §4b convention)

- Live notes LN-4 said "the mechanism may be dead" — **wrong**: 10 `[epistemic]`
  fires prove it runs; only persistence + gate-force are missing.
- Mid-life note "acceleration of memory floor" — **did not hold**; floor was
  reclaimed to ~950 MB by end of life. No leak demonstrated; "designed
  accumulation vs leak" split still owed but non-urgent.
- Early claim "longest life on record" — **wrong**: Run 6 ran 13,341 cycles.
  This is the longest *clean single-segment* life.

---

## Run 11 fix list (per-item observables)

1. **Persist failure at the `steps_unreachable` site** (LN-2). *Observable: no
   `goal_id` appears twice in `failures.jsonl`.*
2. **Miner provenance skip + researchability routing** (LN-1/LN-3): skip
   `unanswered_question` records; route introspective questions to the
   introspection path, not kind=research. *Observable: 0 "Open question:" goals
   sourced from his own speech; failures.jsonl junk class = 0.*
3. **Stamp before archive** (LN-4). *Observable: every completed understanding
   goal in `comp_goals.json` carries `question` + `answered`.*
4. **Diagnose daemon starvation** (finding 4) — instrument goal handoff
   (brain→daemon queue decision log), then fix what it shows. *Observable: no
   WAL silence > 30 min; research-kind records ≥ 20/life.*
5. **Reuse ≥ 8 stands** — downstream of 4; do not re-litigate the target.
   *Observable: ≥8 reuse rows with real cycle+path+topicality.*
6. **Close-out rung 1**: `answered=False` blocks satiety-close for
   understanding goals (or spawns a follow-up goal carrying the question).
   *Observable: 0 understanding goals close with `answered=False` and no
   follow-up; `[epistemic]` NOT-answered count → follow-up goal count matches.*
7. **Question derivation from goal content** (finding 6). *Observable:
   `question_for` outputs vary beyond the "not obvious about X" template on a
   10-goal sample.*
8. **Saturation tripwire: time-at-bound fraction** (finding 7). *Observable:
   harness test fires on a 95 %-at-bound-with-dips series; drive_mastery
   recalibrates ≥1× in a life that reproduces this run's pattern.*
9. **Funnel stages: wire or delete** (finding 8). *Observable: any stage beyond
   `candidate`, or the instrument is gone.*
10. **Zero-with-prejudice harness check** (item h residue). *Observable: a
    blocked action's EMA write path proven in a test, not inferred.*

Out of scope (Ric's open decisions): `genuine_contact` scoring mode;
T0.5 exemplars; the developmental axes (anneal/infancy/houses — post-gate,
`docs/ORRIN_WORLD_DESIGN_2026-07-18.md`).
