# Demo Runs — Index

The permanent behavioral record. Each folder is one captured life (or diagnostic
run) analyzed as a complete set — **never split a single run's docs**. Run analyses
are point-in-time history: they reference whatever docs were live *at the time* and
are left stale on purpose.

*(Rewritten 2026-07-01 as a run index; the original June-17 public-demo-target note
this file replaced is preserved in git history and summarized under "Pending capture
targets" below.)*

## Runs (oldest → newest)

| Run folder | Life | What it captured |
|---|---|---|
| `2026-06-16-run/` | live run, report written mid-run | First full behavioral/introspective report + the learning/selection-mismatch diagnosis (`LEARNING_DIAGNOSIS_2026-06-16.md`). |
| `2026-06-17-run/` | 8,040 cycles | Full forensic read (6 docs): the phantom `action_debt` rut, mode-flap, telemetry-archival fix. Negative result — rut detected, intervention applied, behavior didn't change. |
| `2026-06-18-run/` | ~10,300 cycles | First life after the 06-17 fixes: phantom avoidance loop 2,251→5 cycles, mode-flap 3,415→6, telemetry 240→10,260 pts. Positive mechanism result. |
| `2026-06-19-run/` | 11,633 cycles (~13.9 h) | First life under binding / goal-lens / production machinery. Clean death verified; production gate proven honest (146 effects → 0 credited — negative by design). |
| `2026-06-25-run/` | ~17,352 cycles (~25 h) | First life under the new goal architecture; intent-vs-behaviour companion to `PRE_RUN_NOTE_2026-06-24.md`; before/after for the 06-18 ops + production findings. |
| `2026-06-29-run/` | 1,459 cycles (~47 min, diagnostic) | Short operator-stopped run; "hot and flat" — exposed the diagnosis (appraisal habituation, felt-lexicon membrane, goal-spam reframe) that drove the grounding-and-surface work. Before picture. |
| `2026-07-01-run/` | 14,472 cycles (~21.7 h) | First life under P1 effect-gated goal closure + disengage watchdog; the *after* picture for Phase 1 of the grounding-and-surface plan. |
| `2026-07-02-run/` | 10,071 cycles (~9.2 h) | §8 acceptance Run 2 + Grounding & Surface (P1–P8) / AR1–AR9 staging run (7-doc set incl. deeper pass + data-file audit). Gate NOT passed: S5 significance (1.114) + S9 selection→value moved; S6 aspiration crediting + S7 production lane split failed. Drives now breathe (closes B3). **First captured life with a real interlocutor** — 10 utterances / 10 replies, unregistered by the person model. Second pass found the **rest-drive ignition monopoly** (~74% of cycles at `drive_rest@1.00`, no sleep behavior; consolidation blackout by 10:45) and AR2's missing `ctx.web_search` hook. Includes `data/` capture set. |
| `2026-07-03-run/` | 07-03 life | §8 acceptance Run 3. Gate NOT passed but biggest movement yet (S6 passed; **11 first-ever research memos**). New jammed horn: **social_presence ignition monopoly (84 %)**. Set the Run 4 gate: reuse + desync bridge + EMA authority. |
| `2026-07-05-run/` | 07-05 life (dirty instance) | Run 4 fix build verdict: **S8 PASSED** (0 desyncs / 223 completions), production funnel first nonzero (292/124/28). Blockers found: `compose_section` template stamper (197 KB fake manuscript), aspirations fail-able (`output_producing` killed 104 s before death), learned-note bodies eaten by the pruner, 0 memos so reuse unreachable. |
| `2026-07-08-run/` | 12,330 cycles (clean single segment) | Run 5 (F1–F9 + F10–F22 build, commit `f0c4698`). Gate NOT passed (S6/S9 fail). Produced the goals-system G1–G5 findings + deeper-pass R7–R15; drove the Run 6 plan (value authority + commitment rotation). |
| `2026-07-11-run/` | 13,341 cycles (~15.5 h) | Run 6 (value-authority build `584b76a` + Companion & Presence `e4abfe7`). Gate FAILED, but the failure changed species: `look_outward` 4,899→88 picks (first learned-value kill), while the **QuadRF fetch loop's timestamp footers defeated ledger dedup — 387 credited rewrites pumped `value_ema` to 0.8142 → 92 % monopoly**. 7-doc analysis set incl. self-awareness audit. Drove `RUN7_FIX_PLAN` (make credit un-pumpable). |
| `2026-07-12-run/` | 11,060 cycles (~8 h) | Run 7 (anti-pump build F1–F8, `a63b160`+`bb3685a`). Gate NOT passed (1/4) but the run that *isolated* the cause: pump deflated (`value_ema` 0.5196, no memo loop, max rewrite 2× vs 403×), yet commitment share still **90.9 %** on `self_understanding` (stale_cycles 10,291) — proving the monopoly is structural (relative-penalty saturation + one-member rotation pool), not a reward artifact. Drove `RUN8_FIX_PLAN` F1+F2. |
| `2026-07-15-run/` | 9,785 cycles (**two segments, mid-life crash**) | Run 8 (F1+F2 build `fc2b635`). **The monopoly finally broke: 90.9 % → 42.6 %** (S10 ✅ / G1 ✅), all four aspirations drove the slot, `genuine_contact` committed for the first time. But **F1 never fired** (`refractory_events` empty, max stale 8.8) — **F2's aspiration rotation is the load-bearing lever**; it prevented staleness from accumulating so F1 stayed dormant. Formal gate `G1∧G2∧G4` NOT passed (G2 unmet, F1 unproven; reuse regressed 2<4). Also surfaced+fixed a real crash (`make_candidate` keyword-only dispatch `TypeError`, `invoke.py:108`, fix committed in `e70ac98`; regression test still owed) and a 6.5 h watchdog stall latency. Second-pass audit (verdict §4b): reuse rows *do* resolve to concrete referents via `content_hash` (one cites a prior memo by filename), and both reuse events are time-locked ≤250 ms to goal failures — that reuse↔failure coupling is a Run 9 question alongside the `ORRIN_STALE_REFRACTORY=0` ablation. |
| `2026-07-17-run/` | 10,278 cycles (**two segments, supervisor kill**) | Run 9′ (R9-F1..F7 build). Gate NOT passed as written (reuse **1** < 8) but every Run 9 mechanism proven: **first exemplar promotion in nine lives** (3 promoted), honest failures (0 over-max, 0 flaps, runner race gone), best-ever occupancy **40.5 %**, the single reuse row fully honest (real cycle/path, topical, no failure time-lock). **First life with live resource watchdogs** — the memory guard killed segment 1 at rss 1,898 MB (`HARD:memory_leak_slope`, 726 MB/60 s burst unattributed — no RSS series exists) and the full detect→kill→relaunch→rebirth arc worked. New seam: one question created as two goal ids in two stores (→ both desyncs + double-failure). Run 10 fix list in the run doc. |
| `2026-07-18-run-aborted/` | 2,671 cycles (~1.7 h, single segment, **external kill**) | Run 10 attempt 1, **ABORTED — not scoreable, no verdict**. Build `b175ed2`, symbolic-only. Terminal close at 09:47 → `BrokenPipeError` on the first `print()` in `graceful_shutdown` (`runtime/lifecycle.py:132`, tee pipe already dead) → rest of shutdown skipped **and run-lock left engaged**. No cognitive errors; rotation visible (top occupancy 55.9 %, 5 goals held the slot) but nothing gate-relevant matured. Fix: shutdown prints made pipe-safe. Gate rolls over to the relaunch. |
| `2026-07-18-run/` | **11,565 cycles** (single clean segment, operator stop, **zero crashes/gaps**) | Run 10 (R10-1..12 build, `4d69ce5` — **first reproducible life**), symbolic-only. Gate **NOT passed** (reuse **0** < 8) but 9/12 items green: **originality veto fired in-life** (87 %-verbatim held for review; 5 clean promotions), twin-id seam dead (0 desyncs/flaps), **first 10 outward causal edges**, rule refractory 66,087→374, RSS fully attributed (sawtooth, self-reclaiming floor), occupancy 40.4 % with ignition monopoly also broken (42.3 %), production 101/50/52 artifacts. **Constraint moved:** daemon lane starved (125 WAL events, one 8-h silence) so the proven reuse loop was never fed. Close-out fired 10× — every question **unanswered**, goals closed anyway (annotation → must become gate). Live bugs LN-1..4 all classified (2 broken pipes, 1 unopposed force, 1 misaimed). Run 11 = 10-item list in the run doc. |
| `2026-07-21-run/` | **18,327 cycles** (~29.3 h, single productive segment, natural lifespan death, **longest life yet**) | Run 11 "The Growth Run" (build `423e201`, committed 2 min pre-launch), symbolic-only. Gate (`RUN11_BACKLOG §10`, 4 axes) **NOT passed**. **Health + Honesty best ever:** occupancy **43.3 %** with **all four aspirations driving** and `genuine_contact` committed **14.3 %** (first substantial contact commitment); **miner-junk failures 0** (F-LN1/LN3 fixed); originality veto held 21 scrape promotions; **R9 cycle-stall tripwire fired in a real life for the first time**; funnel wired past candidate; 137/500 outward causal edges. **Feed + Growth failed identically to Run 10:** daemon goal-creation lane went **terminally silent at 09:59Z** (no daemon goal for the final ~12.3 h) → **reuse 0** (3rd straight < 8); symbolic mode never *answers* a question (**answered-rate 0**) so the ladder never ratchets. **New bug:** `graceful_shutdown` **hung after writing death artifacts** at natural death (cycle 18,327) → 4 born-dead relaunch/kill cycles before clean exit. Persisting skeptic reds: `decide_to_write_code` blocked 1,967× yet EMA 0.576/rank 18 (reward pays gesture); `drive_mastery` @1.00 all life, ignition duty 98.2 %. Run 12 = 8-item list in the run doc. |

## Pending capture targets

Carried over from the original demo-target note; not yet captured:

- Rut fix **redistributes effort** (first non-zero effect-ledger row; effort spreads
  to "make things"/"be useful") — follow-ups queued in 2026-06-19 `run_analysis.md` §6.
- Body bands reduce false distress (before/after body-sense run).
- Workspace prior changes action coherence (selector trace with/without prior).
- Home/world zoning changes goal routing (goal tags + routing comparison).
- Live-run UI capture: `docs/images/orrin_learning_run.gif` (the static
  `docs/images/orrin_learning_ui.png` in the README is staged data, not behavioral
  evidence).
