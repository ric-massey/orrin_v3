# Benchmarks & Verification

Benchmarks
- Capability benchmarks: lateral reasoning, goal completion rates, memory retrieval accuracy.

Running benchmarks
- See `tests/benchmarks/` to run the standard suite; capture metrics and compare to baselines.
