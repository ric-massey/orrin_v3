# Peers Subsystem

Implementation
- brain/peers/ with base peer class, peer registry, and proposal router.

Writing a custom peer
- Inherit from PeerBase, implement wake conditions and proposal generation, register in peers/registry.
