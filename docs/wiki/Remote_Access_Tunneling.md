# Remote Access & Tunneling

Security
- Treat remote access tokens as sensitive. Use ephemeral tunnels and secure NAT traversal.

Tunneling
- `expose_orrin.command` provides recommended tunneling setup (Tailscale/ngrok alternatives).
