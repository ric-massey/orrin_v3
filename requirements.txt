# requirements.txt — GENERATED MIRROR of pyproject.toml's `all` extra.
#
# pyproject.toml is the single source of truth (CODEBASE_CLEANUP_PLAN Phase 2).
# Do NOT hand-edit: change the dependency groups in pyproject.toml, then mirror
# the direct deps here. For a fully-pinned, reproducible install, use the
# generated lock instead — requirements.lock (resolved for Linux/CPython 3.11):
#
#   uv pip compile pyproject.toml --all-extras \
#       --python-platform linux --python-version 3.11 --no-header -o requirements.lock
#
# This file lists DIRECT dependencies only (preserving the previous format), so
# `pip install -r requirements.txt` reproduces the full product runtime — the
# same set as `pip install ".[all]"`. For a leaner install, use the extras:
#   pip install -e ".[backend,dev]"          # API + tests, no ML/desktop
#   pip install -e ".[backend,embedding]"    # API + real embeddings

# ── core runtime (brain loop + memory/goals daemons) ──
prometheus_client>=0.20
numpy>=1.26
requests>=2.31
beautifulsoup4>=4.12
watchdog==6.0.0
python-dotenv>=1.0.1
psutil>=5.9

# ── backend (telemetry/control API + websocket) ──
fastapi>=0.111
uvicorn[standard]>=0.30

# ── desktop (native window, tray, OS keychain) ──
pywebview>=5.0
pystray>=0.19
Pillow>=10.0
keyring>=24.0

# ── embedding / nlp (heavyweight; deterministic hash / regex fallbacks if absent) ──
# After install (optional): python -m spacy download en_core_web_sm
sentence-transformers>=2.7
spacy>=3.7

# ── LLM providers (openai default + Local/Custom endpoints; others lazy) ──
openai>=1.40.0
anthropic>=0.40.0
google-genai>=0.3.0
