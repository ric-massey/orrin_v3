"""Runtime-launch helpers extracted from main.py (CODEBASE_CLEANUP_PLAN Phase 4B).

main.py is the orchestrator; the cohesive, side-effect-free helper groups it used
to inline live here so the entrypoint stays a thin boot sequence.
"""
